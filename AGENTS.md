## Agent skills

### Issue tracker

Issues live in GitHub Issues (`SagirisWebDev/dynamo_theme`). See `.agents/docs/agents/issue-tracker.md`.

### Triage labels

Using default label vocabulary (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix). See `.agents/docs/agents/triage-labels.md`.

### Domain docs

Single-context — `.agents/context/CONTEXT.md` + `.agents/docs/adr/` at the repo root. See `.agents/docs/agents/domain.md`.
